const type = new Typed('.typed', {
    strings: [
        '<p class="txtp">MORE THAN A CUSTOMER, A FRIEND.</p>'
    ],

    typeSpeed: 80,
    startDelay: 3000,
    backspeed: 100,
    smartBackspace: true,
    shuffle: false,
    backDelay: 1100,
    showCursor: true,
    cursorChar: ' ',
    contentType: 'html',
});
const duluth = new Typed('.duluth', {
    strings: [
        '<p>DULUTH CAR WASH</p>'
    ],

    typeSpeed: 80,
    startDelay: 300,
    smartBackspace: true,
    shuffle: false,
    backDelay: 1100,
    loop: true,
    loopCount: 1,
    showCursor: true,
    cursorChar: '',
    contentType: 'html',
});
